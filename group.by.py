# select status, count(shipping_id) from Shippings
# group by status    Найдите общее количество доставок для каждого статуса доставки.



# select customer_id, count(order_id) from Orders
# group by customer_id   Требуется подсчитать количество заказов для каждого клиента.


# select customer_id, count(order_id) from Orders
# group by customer_id   уникальный олкого болуп, ошол олкодон эн жаш, эн картан жана орто жашын табуу + столбецтердин аты меникиндей болуш керек


# select item, count(order_id), sum(amount) from Orders
# group by item      кандай уникальный товарлар бар жана канча шт сатылган + общий канча сомго сатылга, столбецтердин аты мисалга окшош болуш керек
